{% extends "simple_template.html.twig" %} 

{% block simplepage %}
{# <h3>J'espère que tu es en forme {{user.name}} !</h3>
{% for add in lastAdds %}
{{add.title}} {{add.name}} {{add.address}} 
{% endfor %} #}

<div class="row h-100 justify-content-center">  
<!--Section 1 left part-->
    <div class="custom_section col-lg-6 d-flex justify-content-center align-items-center bg-light">
        <div class="container w-100 h-75" > 
            <div class="custom_bg_s1_col1 row h-100 text-center">
                <div class="col-md-12 m-auto p-0 bg-primary ">
                    <h2 class="text-dark">{{title}}</h2>
                </div>              
            </div>  
        </div>
    </div>
    <!--Section 1 right part-->
    <div class="custom_section col-lg-6 d-flex justify-content-center align-items-center bg-light">
        <div class="container w-100 h-75" >          
            <div class="custom_bg_s1_col2 row h-100 text-center bg-light">
                <div class="col-lg-12 m-auto">
                    <h2> Connexion</h2>
                    <form class="" method="POST" action="index.php?controller=home&action=login">
                        <div class="form-group">
                            <label for="login"></label>        
                            <input type="email" placeholder="identifiant email" name="login" required>
                            
                            <label for="password"></label>        
                            <input type="password"  placeholder="mot de passe" name="password" required>   
                        </div>       
                         
                        <button class="btn btn-primary" type="submit">Se connecter</button>   
                    </form> 
                </div>              
            </div>  
        </div>
    </div>
</div>

{% endblock %}